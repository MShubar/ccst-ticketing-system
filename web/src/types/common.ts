export type Option = {
    label: string;
    value: string;
  };
  
  export type BaseEntity = {
    id: number;
  };
  
  export type SelectOption = {
    label: string;
    value: string | number;
  };